# planck
Simple python sample to compute planck spectrum in frequency space

run from command line as

    >> execfile("planck.py")
    
or 

    python planck.py
    
Requires:

 - matplotlib
 - numpy
